import os
import streamlit as st

st.title("Debug Percorsi Pannello Skill")

# Percorso attuale di esecuzione
current_path = os.getcwd()
st.write(f"Il terminale sta lavorando in: `{current_path}`")

# Controllo esistenza cartella pages
if os.path.exists("pages"):
    st.success("✅ Cartella 'pages' trovata!")
    
    # Elenco file dentro pages
    files = os.listdir("pages")
    st.write("File trovati in /pages:", files)
    
    if "1_Console.py" in files:
        st.success("🎯 File '1_Console.py' rilevato correttamente!")
    else:
        st.error("❌ Il file '1_Console.py' non è in questa cartella.")
else:
    st.error("❌ Cartella 'pages' NON TROVATA. Assicurati che sia allo stesso livello di questo script.")
